const Strong = ({ children }) => {
    return <a className="capitalize font-medium text-slate-800 hover:text-slate-900 dark:text-slate-100 dark:hover:text-white" href="#0">
        {children}
    </a>
}

export default Strong;
